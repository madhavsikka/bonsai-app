export interface Leaf {
  id: string;
  name: string;
  content: string;
  createdAt: string;
  modifiedAt: string;
}

export interface User {
  id: string;
  name: string;
  email: string;
  createdAt: string;
  updatedAt: string;
}
